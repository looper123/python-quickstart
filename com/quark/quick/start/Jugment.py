# 条件判断语句
# 同一个代码块中必须保持相同的缩进否则会报错

if True:
    total = ['item_one', 'item_two', 'item_three',
             'item_four', 'item_five']
    # print('judge'+'\n'+'format'+'\n')
    # 一行代码无法写下的时候可以用\连接
    print('judge' +
          'format' +
          'test')
else:
    print('false')
